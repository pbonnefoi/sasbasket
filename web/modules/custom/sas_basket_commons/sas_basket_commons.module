<?php

/**
 * Implements hook_theme()
 *
 * @param $existing
 * @param $type
 * @param $theme
 * @param $path
 * @return array
 */
function sas_basket_commons_theme($existing, $type, $theme, $path) {
  return array(
    'plannings' => array(
      'variables' => ['creneaux' => []]
    )
  );
}
